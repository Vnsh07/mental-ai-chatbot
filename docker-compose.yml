services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: mental
      POSTGRES_PASSWORD: mental
      POSTGRES_DB: mental_ai
    ports:
      - "5432:5432"
    volumes:
      - mental_ai_pg:/var/lib/postgresql/data

volumes:
  mental_ai_pg:
